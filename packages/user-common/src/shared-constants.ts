/*
 * This file contains constants which are shared between more than one sub-module
 * e.g. values required by both the server and front.
 */

export const DEFAULT_TENANT_CODE = '__default_tenant__';
